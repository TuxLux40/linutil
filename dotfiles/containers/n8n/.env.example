# ----- n8n on Synology (example env) -----
# Copy to .env and adjust values as needed.

# Networking / system
TZ=Europe/Berlin
N8N_HTTP_PORT=5678

# Domain / reverse proxy
# Use a subdomain of olips.cloud once you decide it, e.g. n8n.olips.cloud
N8N_HOST=n8n.olips.cloud
N8N_PROTOCOL=https
WEBHOOK_URL=https://n8n.olips.cloud/

# Encryption key for credentials/etc. (generate a strong random value)
# Example generation (fish):
#   openssl rand -base64 32 | tr -d '\n'
N8N_ENCRYPTION_KEY=CHANGE_ME

# Data dir on Synology (adjust to your volume/path)
# Common pattern: /volume1/docker/n8n
/volume1/docker/n8n=/volume1/docker/n8n

# Database (Synology MariaDB10)
DB_TYPE=mysql
DB_HOST=os93-nas
DB_HOST_FALLBACK_IP=192.168.178.2
DB_PORT=3306
DB_NAME=n8n
DB_USER=n8n
DB_PASSWORD=CHANGE_ME
